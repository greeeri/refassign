'use client'

import {useEffect,useMemo,useState} from 'react'
import {createClient} from '../lib/supabase/client'

type Level={id:string;name:string}
type Position={id:string;name:string;sport_id:string;sports?:{name:string}|null}
type Rule={id:string;level_id:string|null;position_id:string;max_games_per_day:number;rest_days:number;same_team_days:number;active:boolean}
type RunResult={games_processed:number;assignments_created:number;positions_left_open:number;start_date:string;end_date:string}
const localDate=(d:Date)=>{const y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return `${y}-${m}-${day}`}

export default function AutoAssignManager(){
 const supabase=useMemo(()=>createClient(),[])
 const today=useMemo(()=>new Date(),[]),weekEnd=useMemo(()=>{const d=new Date();d.setDate(d.getDate()+7);return d},[])
 const [levels,setLevels]=useState<Level[]>([]),[positions,setPositions]=useState<Position[]>([]),[rules,setRules]=useState<Rule[]>([]),[levelId,setLevelId]=useState(''),[positionId,setPositionId]=useState(''),[maxGames,setMaxGames]=useState(2),[restDays,setRestDays]=useState(0),[sameTeamDays,setSameTeamDays]=useState(7),[error,setError]=useState(''),[notice,setNotice]=useState(''),[saving,setSaving]=useState(false),[running,setRunning]=useState(false),[startDate,setStartDate]=useState(localDate(today)),[endDate,setEndDate]=useState(localDate(weekEnd)),[result,setResult]=useState<RunResult|null>(null),[canManage,setCanManage]=useState(false)
 async function load(){const {data:u}=await supabase.auth.getUser();if(u.user){const {data:p}=await supabase.from('profiles').select('role').eq('id',u.user.id).maybeSingle();setCanManage(['admin','assignor'].includes(p?.role||''))}const[l,p,r]=await Promise.all([supabase.from('levels').select('id,name').order('name'),supabase.from('sport_positions').select('id,name,sport_id,sports(name)').order('sort_order'),supabase.from('auto_assign_rules').select('*')]);const e=l.error||p.error||r.error;if(e){setError(e.message);return}setLevels((l.data||[]) as Level[]);setPositions((p.data||[]) as unknown as Position[]);setRules((r.data||[]) as Rule[])}
 useEffect(()=>{void load()},[])
 async function save(){if(!positionId){setError('Select a position.');return}setSaving(true);setError('');setNotice('');const existing=rules.find(r=>(r.level_id||'')===levelId&&r.position_id===positionId);const payload={level_id:levelId||null,position_id:positionId,max_games_per_day:maxGames,rest_days:restDays,same_team_days:sameTeamDays,active:true};const response=existing?await supabase.from('auto_assign_rules').update(payload).eq('id',existing.id):await supabase.from('auto_assign_rules').insert(payload);if(response.error)setError(response.error.message);else setNotice('Auto Assign rule saved.');await load();setSaving(false)}
 async function remove(id:string){const{error:e}=await supabase.from('auto_assign_rules').delete().eq('id',id);if(e)setError(e.message);else{setNotice('Rule removed.');await load()}}
 async function runAutoAssign(){if(!canManage){setError('Only Administrators and Assignors can run AutoAssign.');return}if(!startDate||!endDate||endDate<startDate){setError('Choose a valid start and end date.');return}if(!window.confirm(`Run AutoAssign for games from ${startDate} through ${endDate}? Existing assignments will not be replaced.`))return;setRunning(true);setError('');setNotice('');setResult(null);const {data,error:e}=await supabase.rpc('run_auto_assign',{p_start_date:startDate,p_end_date:endDate});if(e)setError(e.message);else{const r=data as RunResult;setResult(r);setNotice(`AutoAssign complete: ${r.assignments_created} assignment${r.assignments_created===1?'':'s'} created. ${r.positions_left_open} position${r.positions_left_open===1?'':'s'} left open for review.`)}setRunning(false)}
 const levelName=(id:string|null)=>id?levels.find(x=>x.id===id)?.name||'Unknown':'All Levels',posName=(id:string)=>{const p=positions.find(x=>x.id===id);return p?`${p.sports?.name||''} — ${p.name}`:id}
 return <><section className="card" style={{border:'2px solid #16a34a'}}><div className="cardHead"><div><h2><span style={{color:'#eab308'}}>⚡</span> Run AutoAssign</h2><p>Fill open assignment slots automatically. Existing assignments are never replaced and new assignments remain Not Published until you review and publish them.</p></div></div>{error&&<div className="errorBox">{error}</div>}{notice&&<div className="loginMessage">{notice}</div>}<div className="formGrid"><label>Start Date<input type="date" value={startDate} onChange={e=>setStartDate(e.target.value)}/></label><label>End Date<input type="date" value={endDate} onChange={e=>setEndDate(e.target.value)}/></label></div><div style={{margin:'12px 0',fontSize:13,lineHeight:1.6}}><b>AutoAssign checks:</b> active officials, sport, league and level eligibility, availability blocks, assignment time conflicts using game length, prior declines, maximum games per day, rest days by level/position, same-team rotation, mentor eligibility, position ranking and workload fairness.</div><button disabled={running||!canManage} onClick={()=>void runAutoAssign()} style={{background:'#16a34a',color:'#fff',border:0,borderRadius:9,padding:'12px 18px',fontWeight:800,cursor:running?'wait':'pointer',fontSize:15}}><span style={{color:'#facc15'}}>⚡</span> {running?'Running AutoAssign…':'Run AutoAssign'}</button>{result&&<div style={{display:'flex',gap:12,flexWrap:'wrap',marginTop:16}}><span className="badge blue">{result.games_processed} games processed</span><span className="badge green">{result.assignments_created} assigned</span><span className="badge yellow">{result.positions_left_open} left open</span></div>}</section><section className="card"><div className="cardHead"><div><h2>Auto Assign Settings</h2><p>Set workload, recovery and team-repeat rules by level and position.</p></div></div><div className="formGrid"><label>Level<select value={levelId} onChange={e=>setLevelId(e.target.value)}><option value="">All Levels</option>{levels.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Position<select value={positionId} onChange={e=>setPositionId(e.target.value)}><option value="">Select position</option>{positions.map(x=><option key={x.id} value={x.id}>{x.sports?.name||'Sport'} — {x.name}</option>)}</select></label><label>Maximum Games Per Day<input type="number" min={1} max={10} value={maxGames} onChange={e=>setMaxGames(Number(e.target.value))}/></label><label>Rest Days<input type="number" min={0} max={30} value={restDays} onChange={e=>setRestDays(Number(e.target.value))}/><small>Minimum full days before/after another assignment for this level and position.</small></label><label>Days Before Same Team Again<input type="number" min={0} max={90} value={sameTeamDays} onChange={e=>setSameTeamDays(Number(e.target.value))}/><small>Prevents repeatedly assigning the same official to either team too soon.</small></label></div><div className="headerActions" style={{marginTop:14}}><button className="primary" disabled={saving} onClick={()=>void save()}>{saving?'Saving…':'Save Auto Assign Rule'}</button></div></section><section className="card"><div className="cardHead"><div><h2>Current Rules</h2><p>A level-specific rule takes priority over an All Levels rule for the same position. If no rule exists, AutoAssign defaults to 2 games/day, 0 rest days and 7 days before seeing the same team.</p></div></div><div className="tableWrap"><table><thead><tr><th>Level</th><th>Position</th><th>Games / Day</th><th>Rest Days</th><th>Same Team</th><th></th></tr></thead><tbody>{rules.length?rules.map(r=><tr key={r.id}><td>{levelName(r.level_id)}</td><td>{posName(r.position_id)}</td><td>{r.max_games_per_day}</td><td>{r.rest_days}</td><td>{r.same_team_days} days</td><td><button className="secondary" onClick={()=>void remove(r.id)}>Remove</button></td></tr>):<tr><td colSpan={6}>No Auto Assign rules yet.</td></tr>}</tbody></table></div></section></>
}
