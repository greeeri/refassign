'use client'
import {ChangeEvent,FormEvent,useEffect,useMemo,useState} from 'react'
import {createClient} from '../lib/supabase/client'
type Sport={id:string;name:string;default_officials:number};type Level={id:string;name:string};type League={id:string;name:string};type Team={id:string;name:string;level:string|null;level_id:string|null;sport_id:string|null};type Venue={id:string;name:string;city:string|null;state:string|null};type GameRow={id:string;sport_id:string;league_id:string|null;home_team_id:string|null;away_team_id:string|null;venue_id:string|null;starts_at:string;level:string|null;officials_needed:number;status:string;sports:Sport|null;leagues:League|null;home:Team|null;away:Team|null;venue:Venue|null};type ImportRow={row:number;sport:string;league:string;level:string;home_team:string;away_team:string;date:string;time:string;venue:string;officials_needed:string;notes:string;valid:boolean;issue:string}
const blank={sport_id:'',league_id:'',level_id:'',home_team_id:'',away_team_id:'',venue_id:'',date:'',time:'',officials_needed:3,notes:''};const REQUIRED=['sport','league','level','home_team','away_team','date','time','venue','officials_needed','notes']
export default function GamesManager(){const supabase=useMemo(()=>createClient(),[]);const [games,setGames]=useState<GameRow[]>([]);const [sports,setSports]=useState<Sport[]>([]);const [levels,setLevels]=useState<Level[]>([]);const [leagues,setLeagues]=useState<League[]>([]);const [teams,setTeams]=useState<Team[]>([]);const [venues,setVenues]=useState<Venue[]>([]);const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [error,setError]=useState('');const [show,setShow]=useState(false);const [form,setForm]=useState(blank);const [filter,setFilter]=useState('All');const [showImport,setShowImport]=useState(false);const [importRows,setImportRows]=useState<ImportRow[]>([]);const [importing,setImporting]=useState(false);const [importMessage,setImportMessage]=useState('')
async function load(){setLoading(true);const [s,l,lg,t,v,g]=await Promise.all([supabase.from('sports').select('id,name,default_officials').eq('active',true).order('name'),supabase.from('levels').select('id,name').eq('active',true).order('name'),supabase.from('leagues').select('id,name').eq('active',true).order('name'),supabase.from('teams').select('id,name,level,level_id,sport_id').order('name'),supabase.from('venues').select('id,name,city,state').order('name'),supabase.from('games').select('id,sport_id,league_id,home_team_id,away_team_id,venue_id,starts_at,level,officials_needed,status,sports(id,name,default_officials),leagues(id,name),home:teams!games_home_team_id_fkey(id,name,level,level_id,sport_id),away:teams!games_away_team_id_fkey(id,name,level,level_id,sport_id),venue:venues(id,name,city,state)').order('starts_at')]);const err=s.error||l.error||lg.error||t.error||v.error||g.error;if(err)setError(err.message);else{setSports(s.data||[]);setLevels(l.data||[]);setLeagues(lg.data||[]);setTeams(t.data||[]);setVenues(v.data||[]);setGames((g.data||[]) as unknown as GameRow[])}setLoading(false)}useEffect(()=>{load()},[])
function chooseSport(id:string){const s=sports.find(x=>x.id===id);setForm(f=>({...f,sport_id:id,home_team_id:'',away_team_id:'',officials_needed:s?.default_officials||1}))}function chooseLevel(id:string){setForm(f=>({...f,level_id:id,home_team_id:'',away_team_id:''}))}const eligibleTeams=teams.filter(t=>t.sport_id===form.sport_id&&t.level_id===form.level_id)
async function save(e:FormEvent){e.preventDefault();setSaving(true);setError('');try{if(form.home_team_id===form.away_team_id)throw new Error('Home and away teams must be different.');const level=levels.find(l=>l.id===form.level_id);if(!level)throw new Error('Select an approved level.');const starts=new Date(`${form.date}T${form.time}:00`).toISOString();const {error}=await supabase.from('games').insert({sport_id:form.sport_id,league_id:form.league_id,level_id:form.level_id,level:level.name,home_team_id:form.home_team_id,away_team_id:form.away_team_id,venue_id:form.venue_id,starts_at:starts,officials_needed:Number(form.officials_needed),notes:form.notes.trim()||null,status:'open'});if(error)throw error;setForm(blank);setShow(false);await load()}catch(err){setError(err instanceof Error?err.message:'Unable to save game')}finally{setSaving(false)}}
function parseCSV(text:string){const rows:string[][]=[];let row:string[]=[];let cell='',quoted=false;for(let i=0;i<text.length;i++){const ch=text[i];if(ch==='"'){if(quoted&&text[i+1]==='"'){cell+='"';i++}else quoted=!quoted}else if(ch===','&&!quoted){row.push(cell.trim());cell=''}else if((ch==='\n'||ch==='\r')&&!quoted){if(ch==='\r'&&text[i+1]==='\n')i++;row.push(cell.trim());cell='';if(row.some(v=>v))rows.push(row);row=[]}else cell+=ch}row.push(cell.trim());if(row.some(v=>v))rows.push(row);return rows}
function validateImport(raw:string[][]){if(!raw.length)throw new Error('The CSV file is empty.');const h=raw[0].map(x=>x.toLowerCase().trim().replace(/\s+/g,'_'));const missing=REQUIRED.filter(x=>!h.includes(x));if(missing.length)throw new Error(`Missing columns: ${missing.join(', ')}`);return raw.slice(1).map((r,i)=>{const get=(x:string)=>r[h.indexOf(x)]?.trim()||'';const sport=get('sport'),league=get('league'),level=get('level'),home=get('home_team'),away=get('away_team'),venue=get('venue'),date=get('date'),time=get('time'),officials=get('officials_needed');const s=sports.find(x=>x.name.toLowerCase()===sport.toLowerCase());const lg=leagues.find(x=>x.name.toLowerCase()===league.toLowerCase());const l=levels.find(x=>x.name.toLowerCase()===level.toLowerCase());const homeObj=teams.find(x=>x.name.toLowerCase()===home.toLowerCase()&&x.sport_id===s?.id&&x.level_id===l?.id);const awayObj=teams.find(x=>x.name.toLowerCase()===away.toLowerCase()&&x.sport_id===s?.id&&x.level_id===l?.id);const venueObj=venues.find(x=>x.name.toLowerCase()===venue.toLowerCase());const issues:string[]=[];if(!s)issues.push('Sport not found');if(!lg)issues.push('League not in Game Setup');if(!l)issues.push('Level not in Game Setup');if(!homeObj)issues.push('Home team not in Game Setup for sport/level');if(!awayObj)issues.push('Away team not in Game Setup for sport/level');if(!venueObj)issues.push('Venue not in Game Setup');if(home&&away&&home.toLowerCase()===away.toLowerCase())issues.push('Home/away must differ');if(!/^\d{4}-\d{2}-\d{2}$/.test(date))issues.push('Date must be YYYY-MM-DD');if(!/^([01]\d|2[0-3]):[0-5]\d$/.test(time))issues.push('Time must be HH:MM');if(!Number.isInteger(Number(officials))||Number(officials)<1||Number(officials)>20)issues.push('Officials needed must be 1-20');return{row:i+2,sport,league,level,home_team:home,away_team:away,date,time,venue,officials_needed:officials,notes:get('notes'),valid:!issues.length,issue:issues.join('; ')}})}
async function handleFile(e:ChangeEvent<HTMLInputElement>){setError('');setImportMessage('');setImportRows([]);const f=e.target.files?.[0];if(!f)return;try{setImportRows(validateImport(parseCSV(await f.text())))}catch(err){setError(err instanceof Error?err.message:'Unable to read CSV')}finally{e.target.value=''}}function downloadTemplate(){const csv='sport,league,level,home_team,away_team,date,time,venue,officials_needed,notes\nSoccer,Approved League,U19,Approved Home Team,Approved Away Team,2026-09-01,19:00,Approved Venue,3,Conference game\n';const b=new Blob([csv],{type:'text/csv'}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download='refassign-game-import-template.csv';a.click();URL.revokeObjectURL(u)}
async function importGames(){setImporting(true);setError('');let added=0,skipped=0;try{for(const r of importRows){if(!r.valid)continue;const s=sports.find(x=>x.name.toLowerCase()===r.sport.toLowerCase())!,lg=leagues.find(x=>x.name.toLowerCase()===r.league.toLowerCase())!,l=levels.find(x=>x.name.toLowerCase()===r.level.toLowerCase())!,home=teams.find(x=>x.name.toLowerCase()===r.home_team.toLowerCase()&&x.sport_id===s.id&&x.level_id===l.id)!,away=teams.find(x=>x.name.toLowerCase()===r.away_team.toLowerCase()&&x.sport_id===s.id&&x.level_id===l.id)!,venue=venues.find(x=>x.name.toLowerCase()===r.venue.toLowerCase())!;const starts=new Date(`${r.date}T${r.time}:00`).toISOString();const {data:existing}=await supabase.from('games').select('id').eq('sport_id',s.id).eq('home_team_id',home.id).eq('away_team_id',away.id).eq('starts_at',starts).maybeSingle();if(existing){skipped++;continue}const {error}=await supabase.from('games').insert({sport_id:s.id,league_id:lg.id,level_id:l.id,level:l.name,home_team_id:home.id,away_team_id:away.id,venue_id:venue.id,starts_at:starts,officials_needed:Number(r.officials_needed),notes:r.notes||null,status:'open'});if(error)throw error;added++}setImportMessage(`Import complete: ${added} added${skipped?`, ${skipped} duplicates skipped`:''}.`);setImportRows([]);await load()}catch(err){setError(err instanceof Error?err.message:'Import failed')}finally{setImporting(false)}}
const visible=filter==='All'?games:games.filter(g=>g.sports?.name===filter),invalid=importRows.filter(r=>!r.valid).length
return <section className="card"><div className="cardHead"><div><h2>Live Game Schedule</h2><p>{games.length} games in Supabase</p></div><div className="headerActions"><button className="secondary" onClick={()=>{setShowImport(!showImport);setShow(false)}}>{showImport?'Close Import':'Import CSV'}</button><button className="primary" onClick={()=>{setShow(!show);setShowImport(false)}}>{show?'Cancel':'+ Add Game'}</button></div></div><div className="toolbar"><select value={filter} onChange={e=>setFilter(e.target.value)}><option>All</option>{sports.map(s=><option key={s.id}>{s.name}</option>)}</select></div>{showImport&&<div className="importPanel"><div className="cardHead"><div><h3>Bulk Schedule Import</h3><p>League, level, teams and venue must exactly match approved Game Setup records.</p></div><button className="secondary" onClick={downloadTemplate}>Download CSV Template</button></div><input type="file" accept=".csv,text/csv" onChange={handleFile}/>{importRows.length>0&&<><div className="tableWrap"><table><thead><tr><th>Row</th><th>Date/Time</th><th>Game</th><th>League</th><th>Sport</th><th>Venue</th><th>Validation</th></tr></thead><tbody>{importRows.map(r=><tr key={r.row}><td>{r.row}</td><td>{r.date}<small>{r.time}</small></td><td>{r.home_team} vs {r.away_team}<small>{r.level}</small></td><td>{r.league}</td><td>{r.sport}</td><td>{r.venue}</td><td><span className={`badge ${r.valid?'green':'red'}`}>{r.valid?'Ready':r.issue}</span></td></tr>)}</tbody></table></div><button className="primary" disabled={importing||invalid>0} onClick={importGames}>{importing?'Importing…':`Import ${importRows.length} Games`}</button></>}</div>}{show&&<form className="officialForm" onSubmit={save}><label>Sport<select required value={form.sport_id} onChange={e=>chooseSport(e.target.value)}><option value="">Select sport</option>{sports.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label>League<select required value={form.league_id} onChange={e=>setForm({...form,league_id:e.target.value})}><option value="">Select approved league</option>{leagues.map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select></label><label>Level<select required value={form.level_id} onChange={e=>chooseLevel(e.target.value)}><option value="">Select approved level</option>{levels.map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select></label><label>Home Team<select required value={form.home_team_id} onChange={e=>setForm({...form,home_team_id:e.target.value})}><option value="">Select approved team</option>{eligibleTeams.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label>Away Team<select required value={form.away_team_id} onChange={e=>setForm({...form,away_team_id:e.target.value})}><option value="">Select approved team</option>{eligibleTeams.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label>Date<input type="date" required value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></label><label>Time<input type="time" required value={form.time} onChange={e=>setForm({...form,time:e.target.value})}/></label><label>Venue<select required value={form.venue_id} onChange={e=>setForm({...form,venue_id:e.target.value})}><option value="">Select approved venue</option>{venues.map(v=><option key={v.id} value={v.id}>{v.name}{v.city?` — ${v.city}`:''}</option>)}</select></label><label>Officials needed<input type="number" min="1" max="20" required value={form.officials_needed} onChange={e=>setForm({...form,officials_needed:Number(e.target.value)})}/></label><label>Notes<input value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><button className="primary" disabled={saving}>{saving?'Saving…':'Save Game'}</button></form>}{error&&<div className="errorBox">{error}</div>}{importMessage&&<div className="loginMessage">{importMessage}</div>}{loading?<p>Loading games…</p>:<div className="tableWrap"><table><thead><tr><th>Date</th><th>Game</th><th>League / Level</th><th>Sport</th><th>Location</th><th>Officials</th><th>Status</th></tr></thead><tbody>{visible.map(g=>{const d=new Date(g.starts_at);return <tr key={g.id}><td>{d.toLocaleDateString()}<small>{d.toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})}</small></td><td><b>{g.home?.name||'TBD'} vs {g.away?.name||'TBD'}</b></td><td>{g.leagues?.name||'—'}<small>{g.level||''}</small></td><td>{g.sports?.name||'—'}</td><td>{g.venue?.name||'TBD'}<small>{[g.venue?.city,g.venue?.state].filter(Boolean).join(', ')}</small></td><td>0/{g.officials_needed}</td><td><span className="badge red">Needs officials</span></td></tr>})}</tbody></table></div>}</section>}
