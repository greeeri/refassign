"use client";
import {FormEvent,useEffect,useMemo,useState} from "react";
import {createClient} from "../lib/supabase/client";
type Official={id:string;first_name:string;last_name:string;email:string|null};type Member={official_id:string};type Module={id:string;title:string;description:string;category:string;resource_url:string|null;required:boolean;active:boolean;delivery_type:string;registration_url:string|null;payment_required:boolean;payment_url:string|null;instructor_official_id:string|null;course_start_at:string|null;course_end_at:string|null};type TrainingRegistration={module_id:string;status:string;payment_status:string};type StoredFile={name:string;id:string|null;created_at:string|null;metadata?:{size?:number}|null};
const trainingLibraryCategories=["Laws of the Game","Positioning","Foul Recognition","Assistant Referee","Communication","Game Management","Fitness","Professionalism"];
const localInput=(value:string|null)=>value?new Date(new Date(value).getTime()-new Date(value).getTimezoneOffset()*60000).toISOString().slice(0,16):"";
const iso=(value:FormDataEntryValue|null)=>value?new Date(String(value)).toISOString():null;
export default function IowaSoccerDevelopmentAdmin(){const supabase=useMemo(()=>createClient(),[]),[programId,setProgramId]=useState(""),[officials,setOfficials]=useState<Official[]>([]),[members,setMembers]=useState<Member[]>([]),[modules,setModules]=useState<Module[]>([]),[registrations,setRegistrations]=useState<TrainingRegistration[]>([]),[files,setFiles]=useState<StoredFile[]>([]),[selectedOfficial,setSelectedOfficial]=useState(""),[deliveryType,setDeliveryType]=useState("self_led"),[paymentRequired,setPaymentRequired]=useState(false),[editingId,setEditingId]=useState(""),[editDeliveryType,setEditDeliveryType]=useState("self_led"),[editPaymentRequired,setEditPaymentRequired]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(""),[notice,setNotice]=useState("");
 async function loadFiles(){const{data,error:e}=await supabase.storage.from("iowa-training-materials").list("materials",{limit:100,sortBy:{column:"name",order:"asc"}});if(e)setError(e.message);else setFiles((data||[]) as StoredFile[])}
 async function load(){setError("");const{data:program,error:pe}=await supabase.from("registration_programs").select("id").eq("slug","iowa-soccer").single();if(pe)return setError(pe.message);setProgramId(program.id);const[o,m,t,r]=await Promise.all([supabase.rpc("list_development_program_officials",{p_program_id:program.id}),supabase.from("registration_program_officials").select("official_id").eq("program_id",program.id),supabase.from("development_modules").select("id,title,description,category,resource_url,required,active,delivery_type,registration_url,payment_required,payment_url,instructor_official_id,course_start_at,course_end_at").eq("program_id",program.id).order("sort_order"),supabase.from("development_training_registrations").select("module_id,status,payment_status")]);const e=o.error||m.error||t.error||r.error;if(e)return setError(e.message);setOfficials((o.data||[]) as Official[]);setMembers((m.data||[]) as Member[]);setModules((t.data||[]) as Module[]);setRegistrations((r.data||[]) as TrainingRegistration[]);await loadFiles()}useEffect(()=>{void load()},[]);
 async function addOfficial(){if(!selectedOfficial||!programId)return;setBusy(true);setError("");setNotice("");const{data:{user}}=await supabase.auth.getUser();const{error:e}=await supabase.from("registration_program_officials").insert({program_id:programId,official_id:selectedOfficial,source:"registrar",added_by:user?.id||null});if(e)setError(e.message);else{setNotice("Official added to Iowa Soccer Development.");setSelectedOfficial("");await load()}setBusy(false)}
 async function removeOfficial(id:string){if(!window.confirm("Remove this official's Iowa Soccer Development access?"))return;const{error:e}=await supabase.from("registration_program_officials").delete().eq("program_id",programId).eq("official_id",id);if(e)setError(e.message);else await load()}
 function modulePayload(form:FormData,type:string,pay:boolean){const scheduled=type!=="self_led";return{title:form.get("title"),category:form.get("category"),description:form.get("description")||"",resource_url:form.get("resource_url")||null,required:form.get("required")==="on",delivery_type:type,registration_url:scheduled?(form.get("registration_url")||null):null,payment_required:type==="in_person"&&pay,payment_url:type==="in_person"&&pay?(form.get("payment_url")||null):null,instructor_official_id:scheduled?(form.get("instructor_official_id")||null):null,course_start_at:scheduled?iso(form.get("course_start_at")):null,course_end_at:scheduled?iso(form.get("course_end_at")):null}}
 async function addModule(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");setNotice("");const form=new FormData(event.currentTarget),payload=modulePayload(form,deliveryType,paymentRequired);if(deliveryType!=="self_led"&&!payload.course_start_at){setError("Date and start time are required for Virtual and In-Person courses.");setBusy(false);return}const{data:{user}}=await supabase.auth.getUser();const{error:e}=await supabase.from("development_modules").insert({...payload,program_id:programId,sort_order:modules.length*10+10,created_by:user?.id||null});if(e)setError(e.message);else{setNotice("Training module added.");event.currentTarget.reset();setDeliveryType("self_led");setPaymentRequired(false);await load()}setBusy(false)}
 function beginEdit(m:Module){setEditingId(m.id);setEditDeliveryType(m.delivery_type||"self_led");setEditPaymentRequired(Boolean(m.payment_required));setError("");setNotice("")}
 async function saveModule(event:FormEvent<HTMLFormElement>,id:string){event.preventDefault();setBusy(true);setError("");setNotice("");const form=new FormData(event.currentTarget),payload=modulePayload(form,editDeliveryType,editPaymentRequired);if(editDeliveryType!=="self_led"&&!payload.course_start_at){setError("Date and start time are required for Virtual and In-Person courses.");setBusy(false);return}const{error:e}=await supabase.from("development_modules").update({...payload,active:form.get("active")==="on",updated_at:new Date().toISOString()}).eq("id",id);if(e)setError(e.message);else{setNotice("Training module updated.");setEditingId("");await load()}setBusy(false)}
 async function uploadMaterial(event:FormEvent<HTMLFormElement>){event.preventDefault();const input=event.currentTarget.elements.namedItem("material") as HTMLInputElement,file=input.files?.[0];if(!file)return;setBusy(true);setError("");setNotice("");const safe=file.name.replace(/[^a-zA-Z0-9._-]+/g,"-");const path=`materials/${Date.now()}-${safe}`;const{error:e}=await supabase.storage.from("iowa-training-materials").upload(path,file,{upsert:false});if(e)setError(e.message);else{setNotice(`${file.name} uploaded to the Training Materials Library.`);event.currentTarget.reset();await loadFiles()}setBusy(false)}
 async function deleteMaterial(name:string){if(!window.confirm("Delete this training material?"))return;const{error:e}=await supabase.storage.from("iowa-training-materials").remove([`materials/${name}`]);if(e)setError(e.message);else await loadFiles()}
 async function postAnnouncement(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const form=new FormData(event.currentTarget);const{data:{user}}=await supabase.auth.getUser();const{error:e}=await supabase.from("development_announcements").insert({program_id:programId,title:form.get("title"),message:form.get("message"),created_by:user?.id||null});if(e)setError(e.message);else{setNotice("Program announcement posted.");event.currentTarget.reset()}setBusy(false)}
 const memberIds=new Set(members.map(m=>m.official_id)),available=officials.filter(o=>!memberIds.has(o.id)),confirmedIds=new Set(registrations.filter(r=>r.status==="approved"||r.payment_status==="paid").map(r=>r.module_id)),unassigned=modules.filter(m=>m.delivery_type!=="self_led"&&confirmedIds.has(m.id)&&!m.instructor_official_id);function fileUrl(name:string){return supabase.storage.from("iowa-training-materials").getPublicUrl(`materials/${name}`).data.publicUrl}const deliveryLabel=(d:string)=>d==="in_person"?"In Person":d==="virtual"?"Virtual":"Self-Led",instructorName=(id:string|null)=>{const o=officials.find(x=>x.id===id);return o?`${o.first_name} ${o.last_name}`:"Not Assigned"},schedule=(m:Module)=>m.course_start_at?new Date(m.course_start_at).toLocaleString():"Date/Time Not Set";
 const instructorSelect=(name:string,defaultValue="")=><label>Instructor<select name={name} defaultValue={defaultValue}><option value="">Not Assigned</option>{officials.map(o=><option key={o.id} value={o.id}>{o.first_name} {o.last_name}</option>)}</select></label>;
 return <><section className="card"><div className="cardHead"><div><h2>Iowa Soccer Development Administration</h2><p>Add participating officials and manage their training experience.</p></div></div>{error&&<div className="errorBox">{error}</div>}{notice&&<div className="loginMessage">{notice}</div>}{unassigned.length>0&&<div className="errorBox"><b>Instructor Needed:</b> {unassigned.map(m=>m.title).join(", ")} {unassigned.length===1?"has":"have"} confirmed registration but no instructor assigned.</div>}<div className="toolbar"><label>Add Existing Official<select value={selectedOfficial} onChange={e=>setSelectedOfficial(e.target.value)}><option value="">Select an official</option>{available.map(o=><option value={o.id} key={o.id}>{o.last_name}, {o.first_name}</option>)}</select></label><button className="primary" disabled={busy||!selectedOfficial} onClick={()=>void addOfficial()}>Add to Program</button></div></section>
 <section className="card"><h2>Training Content</h2><form className="officialForm" onSubmit={addModule}><label>Module Title<input name="title" required/></label><label>Training Library Section<select name="category" required defaultValue=""><option value="" disabled>Select a section</option>{trainingLibraryCategories.map(c=><option key={c}>{c}</option>)}</select></label><label>Training Type<select value={deliveryType} onChange={e=>{setDeliveryType(e.target.value);if(e.target.value!=="in_person")setPaymentRequired(false)}}><option value="self_led">Self-Led Course</option><option value="virtual">Virtual Training</option><option value="in_person">In-Person Training</option></select></label>{deliveryType!=="self_led"&&<>{instructorSelect("instructor_official_id")}<label>Course Date & Start Time<input name="course_start_at" type="datetime-local" required/></label><label>Course End Time<input name="course_end_at" type="datetime-local"/></label></>}<label className="fullSpan">Description<textarea name="description" required/></label><label className="fullSpan">Training Resource Link<input name="resource_url" type="url"/></label>{deliveryType!=="self_led"&&<label className="fullSpan">Registration Link<input name="registration_url" type="url"/></label>}{deliveryType==="in_person"&&<><label><input type="checkbox" checked={paymentRequired} onChange={e=>setPaymentRequired(e.target.checked)}/> Payment Required</label>{paymentRequired&&<label className="fullSpan">Payment Link<input name="payment_url" type="url" required/></label>}</>}<label><input name="required" type="checkbox"/> Required Training</label><button className="primary" disabled={busy}>{busy?"Adding…":"Add Training Module"}</button></form><div className="developmentRoster">{modules.map(m=><div key={m.id} style={{display:"block"}}>{editingId===m.id?<form className="officialForm" onSubmit={e=>saveModule(e,m.id)}><label>Module Title<input name="title" defaultValue={m.title} required/></label><label>Training Library Section<select name="category" defaultValue={m.category} required>{!trainingLibraryCategories.includes(m.category)&&<option value={m.category}>{m.category} (Current)</option>}{trainingLibraryCategories.map(c=><option key={c}>{c}</option>)}</select></label><label>Training Type<select value={editDeliveryType} onChange={e=>{setEditDeliveryType(e.target.value);if(e.target.value!=="in_person")setEditPaymentRequired(false)}}><option value="self_led">Self-Led Course</option><option value="virtual">Virtual Training</option><option value="in_person">In-Person Training</option></select></label>{editDeliveryType!=="self_led"&&<>{instructorSelect("instructor_official_id",m.instructor_official_id||"")}<label>Course Date & Start Time<input name="course_start_at" type="datetime-local" defaultValue={localInput(m.course_start_at)} required/></label><label>Course End Time<input name="course_end_at" type="datetime-local" defaultValue={localInput(m.course_end_at)}/></label></>}<label className="fullSpan">Description<textarea name="description" defaultValue={m.description} required/></label><label className="fullSpan">Training Resource Link<input name="resource_url" type="url" defaultValue={m.resource_url||""}/></label>{editDeliveryType!=="self_led"&&<label className="fullSpan">Registration Link<input name="registration_url" type="url" defaultValue={m.registration_url||""}/></label>}{editDeliveryType==="in_person"&&<><label><input type="checkbox" checked={editPaymentRequired} onChange={e=>setEditPaymentRequired(e.target.checked)}/> Payment Required</label>{editPaymentRequired&&<label className="fullSpan">Payment Link<input name="payment_url" type="url" defaultValue={m.payment_url||""} required/></label>}</>}<label><input name="required" type="checkbox" defaultChecked={m.required}/> Required Training</label><label><input name="active" type="checkbox" defaultChecked={m.active}/> Active / Visible</label><div style={{display:"flex",gap:8}}><button className="primary" disabled={busy}>Save Changes</button><button type="button" className="secondary" onClick={()=>setEditingId("")}>Cancel</button></div></form>:<div style={{display:"flex",alignItems:"center",justifyContent:"space-between",gap:12}}><span><b>{m.title}</b><small>Training Library: {m.category} • {deliveryLabel(m.delivery_type)}{m.delivery_type!=="self_led"?` • ${schedule(m)} • Instructor: ${instructorName(m.instructor_official_id)}`:""}{m.payment_required?" • Payment Required":""}</small>{confirmedIds.has(m.id)&&m.delivery_type!=="self_led"&&!m.instructor_official_id&&<small style={{display:"block",fontWeight:800,color:"#b42318"}}>⚠ Confirmed registration — instructor required</small>}</span><span style={{display:"flex",alignItems:"center",gap:8}}><span className={`badge ${m.active?"green":"yellow"}`}>{m.active?"Active":"Hidden"}</span><button className="secondary" onClick={()=>beginEdit(m)}>Edit Course</button></span></div>}</div>)}</div></section>
 <section className="card"><div className="trainingSectionHead"><div><h2>Training Materials Library</h2><p>Upload and store PDFs, videos, presentations, documents, and images for Iowa Soccer training.</p></div></div><form className="trainingAdminUpload" onSubmit={uploadMaterial}><label className="fullSpan">Choose Material<input name="material" type="file" required accept=".pdf,.mp4,.webm,.png,.jpg,.jpeg,.webp,.pptx,.docx"/></label><button className="primary" disabled={busy}>Upload Material</button><small>Maximum file size: 500 MB.</small></form><div className="trainingAdminFiles">{files.length?files.map(f=><div key={f.name}><span><b>{f.name.replace(/^\d+-/,"")}</b><small>{f.metadata?.size?` • ${(f.metadata.size/1024/1024).toFixed(1)} MB`:""}</small></span><span><a className="secondary" href={fileUrl(f.name)} target="_blank" rel="noreferrer">Open</a> <button className="secondary" onClick={()=>void deleteMaterial(f.name)}>Delete</button></span></div>):<p>No materials uploaded yet.</p>}</div></section><section className="card"><h2>Post Program Update</h2><form className="officialForm" onSubmit={postAnnouncement}><label>Title<input name="title" required/></label><label className="fullSpan">Message<textarea name="message" required/></label><button className="primary" disabled={busy}>Post Announcement</button></form></section></>}
