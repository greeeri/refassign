'use client'
import {useEffect,useRef} from 'react'
import GamesManagerV3 from './GamesManagerV3'

function value(row:HTMLTableRowElement,index:number){const cell=row.cells[index];if(!cell)return'';const text=(cell.innerText||'').trim();if(index===1){const lines=text.split('\n').map(x=>x.trim()).filter(Boolean);const d=new Date(`${lines[0]||''} ${lines[1]||''}`);if(!Number.isNaN(d.getTime()))return d.getTime()}if(index===5||index===6){const n=parseFloat(text);if(!Number.isNaN(n))return n}return text.toLowerCase()}
export default function SortableGamesManager(){const root=useRef<HTMLDivElement>(null);useEffect(()=>{const setup=()=>{const tables=Array.from(root.current?.querySelectorAll('table')||[]);const table=tables.find(t=>Array.from(t.querySelectorAll('thead th')).some(th=>th.textContent?.trim()==='Game #')&&Array.from(t.querySelectorAll('thead th')).some(th=>th.textContent?.trim()==='Officials'));if(!table||table.getAttribute('data-sort-ready')==='1')return;table.setAttribute('data-sort-ready','1');const headers=Array.from(table.querySelectorAll('thead th')) as HTMLTableCellElement[];let active=-1,dir:1|-1=1;headers.forEach((th,index)=>{if(index===headers.length-1)return;th.style.cursor='pointer';th.style.userSelect='none';th.title='Click to sort ascending or descending';const base=(th.textContent||'').trim();th.textContent=`${base} ↕`;th.addEventListener('click',()=>{dir=active===index?(dir===1?-1:1):1;active=index;headers.forEach((h,i)=>{if(i===headers.length-1)return;const label=(h.textContent||'').replace(/\s[↕▲▼]$/,'');h.textContent=`${label}${i===active?(dir===1?' ▲':' ▼'):' ↕'}`});const body=table.querySelector('tbody');if(!body)return;const rows=Array.from(body.querySelectorAll('tr')) as HTMLTableRowElement[];rows.sort((a,b)=>{const av=value(a,index),bv=value(b,index);return (av<bv?-1:av>bv?1:0)*dir});rows.forEach(r=>body.appendChild(r))})})};setup();const observer=new MutationObserver(setup);if(root.current)observer.observe(root.current,{childList:true,subtree:true});return()=>observer.disconnect()},[]);return <div ref={root}><GamesManagerV3/></div>}
