'use client'
import {useEffect,useMemo,useState} from 'react'
import {createClient} from '../lib/supabase/client'
type Row={assignment_id:string;position_name:string|null;starts_at:string;home_team:string|null;away_team:string|null;location_name:string|null;location_address:string|null;location_city:string|null;location_state:string|null;league_name:string|null;level_name:string|null;notes:string|null;status:string;published_at:string;accept_by:string|null;responded_at:string|null;decline_reason:string|null;response_token:string}
export default function OfficialDashboard({onNavigate}:{onNavigate:(section:string)=>void}){const supabase=useMemo(()=>createClient(),[]);const [rows,setRows]=useState<Row[]>([]),[error,setError]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{async function load(){const {data,error:e}=await supabase.rpc('my_official_assignments');if(e)setError(e.message);else setRows((data||[]) as Row[]);setLoading(false)}void load()},[supabase]);const now=Date.now(),future=rows.filter(r=>new Date(r.starts_at).getTime()>=now).sort((a,b)=>+new Date(a.starts_at)-+new Date(b.starts_at)),needs=future.filter(r=>r.status==='proposed'),accepted=future.filter(r=>['accepted','confirmed'].includes(r.status)),next=accepted[0]||future[0];return <><section className="card"><div className="cardHead"><div><h2>Official Dashboard</h2><p>Your assignments, responses and schedule in one place.</p></div></div>{error&&<div className="errorBox">{error}</div>}{loading?<p>Loading your schedule…</p>:<div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:12}}><button className="card" style={{textAlign:'left',cursor:'pointer'}} onClick={()=>onNavigate('My Assignments')}><small>ACTION REQUIRED</small><div style={{fontSize:30,fontWeight:900}}>{needs.length}</div><b>Assignments need response</b></button><button className="card" style={{textAlign:'left',cursor:'pointer'}} onClick={()=>onNavigate('My Assignments')}><small>UPCOMING</small><div style={{fontSize:30,fontWeight:900}}>{accepted.length}</div><b>Accepted games</b></button><button className="card" style={{textAlign:'left',cursor:'pointer'}} onClick={()=>onNavigate('My Availability')}><small>AVAILABILITY</small><div style={{fontSize:30,fontWeight:900}}>＋</div><b>Add or review blocks</b></button></div>}</section>{next&&<section className="card"><div className="cardHead"><div><h2>Next Game</h2><p>{new Date(next.starts_at).toLocaleString()}</p></div><span className={`badge ${['accepted','confirmed'].includes(next.status)?'green':'yellow'}`}>{['accepted','confirmed'].includes(next.status)?'Accepted':'Needs Response'}</span></div><h3>{next.home_team||'TBD'} vs {next.away_team||'TBD'}</h3><p><b>{next.position_name||'Official'}</b> • {next.location_name||'TBD'}{next.location_city?` • ${next.location_city}${next.location_state?`, ${next.location_state}`:''}`:''}</p>{next.response_token&&<a className="tableButton" href={`/assignment/${next.response_token}`}>View Assignment</a>}</section>}<section className="card"><div className="cardHead"><div><h2>Upcoming Schedule</h2><p>Your next published assignments.</p></div><button className="secondary" onClick={()=>onNavigate('My Assignments')}>View All</button></div>{future.length?<div className="tableWrap"><table><thead><tr><th>Date</th><th>Game</th><th>Position</th><th>Location</th><th>Status</th></tr></thead><tbody>{future.slice(0,6).map(r=><tr key={r.assignment_id}><td>{new Date(r.starts_at).toLocaleDateString()}<small>{new Date(r.starts_at).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})}</small></td><td><b>{r.home_team||'TBD'} vs {r.away_team||'TBD'}</b></td><td>{r.position_name||'Official'}</td><td>{r.location_name||'TBD'}</td><td><span className={`badge ${['accepted','confirmed'].includes(r.status)?'green':'yellow'}`}>{['accepted','confirmed'].includes(r.status)?'Accepted':'Needs Response'}</span></td></tr>)}</tbody></table></div>:<div className="emptyState"><p>No upcoming assignments.</p></div>}</section></>}
