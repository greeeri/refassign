"use client";
import { ChangeEvent, useEffect, useMemo, useState } from "react";
import { createClient } from "../lib/supabase/client";

const COLUMNS = [
  ["USSF-ID", "ussf_id"], ["First name", "first_name"], ["Last name", "last_name"], ["DOB", "date_of_birth"],
  ["is a minor", "is_minor"], ["Gender", "gender"], ["Ethnicity", "ethnicity"], ["Email", "email"],
  ["Secondary email", "secondary_email"], ["Address", "home_address"], ["Apt/suite/unit", "address_unit"],
  ["City", "home_city"], ["State", "home_state"], ["Zip", "home_zip"], ["Country", "country"],
  ["Phone", "phone"], ["Mobile phone", "mobile_phone"], ["License", "license"], ["Status", "license_status"],
  ["Issue date", "license_issue_date"], ["Expiration date", "license_expiration_date"], ["Issuer", "license_issuer"],
  ["Curriculum", "curriculum"], ["Background screening", "background_screening"],
  ["Background screening expiration date", "background_screening_expiration_date"], ["SafeSport", "safesport"],
  ["SafeSport expiration date", "safesport_expiration_date"], ["Intro to Player Safety", "intro_player_safety"],
  ["Intro to Player Safety expiration date", "intro_player_safety_expiration_date"], ["Safe Soccer", "safe_soccer"],
  ["Safe Soccer expiration date", "safe_soccer_expiration_date"], ["Provisional Status", "provisional_status"],
  ["Referee years exp", "referee_years_experience"],
] as const;
type Key = typeof COLUMNS[number][1];
type Official = { id:string } & Record<Exclude<Key,"ussf_id">, string|number|boolean|null>;
type Row = Record<Key,string> & { row:number; id:string; action:"Add"|"Update"; valid:boolean; issue:string };
const normalize=(value:string)=>value.trim().toLowerCase().replace(/[^a-z0-9]+/g,"_").replace(/^_|_$/g,"");
const aliases:Record<string,Key>=Object.fromEntries(COLUMNS.flatMap(([label,key])=>[[normalize(label),key],[normalize(key),key]])) as Record<string,Key>;
const esc=(value:unknown)=>{const text=String(value??"");return /[",\n]/.test(text)?`"${text.replace(/"/g,'""')}"`:text};
const dateValue=(value:unknown)=>value?String(value).slice(0,10):"";
const booleanValue=(value:string)=>["true","yes","1","y"].includes(value.trim().toLowerCase());
const booleanValid=(value:string)=>!value.trim()||["true","false","yes","no","1","0","y","n"].includes(value.trim().toLowerCase());
function parseCSV(text:string){const rows:string[][]=[];let row:string[]=[],cell="",quoted=false;for(let i=0;i<text.length;i++){const ch=text[i];if(ch==='"'){if(quoted&&text[i+1]==='"'){cell+='"';i++}else quoted=!quoted}else if(ch===","&&!quoted){row.push(cell);cell=""}else if((ch==="\n"||ch==="\r")&&!quoted){if(ch==="\r"&&text[i+1]==="\n")i++;row.push(cell);cell="";if(row.some(v=>v.trim()))rows.push(row);row=[]}else cell+=ch}row.push(cell);if(row.some(v=>v.trim()))rows.push(row);return rows}
function validDate(value:string){return !value||(/^\d{4}-\d{2}-\d{2}$/.test(value)&&!Number.isNaN(Date.parse(`${value}T00:00:00Z`)))}

export default function OfficialsRosterManager({organizationId}:{organizationId?:string}){
 const supabase=useMemo(()=>createClient(),[]),[allowed,setAllowed]=useState(false),[officials,setOfficials]=useState<Official[]>([]),[rows,setRows]=useState<Row[]>([]),[error,setError]=useState(""),[message,setMessage]=useState(""),[importing,setImporting]=useState(false);
 async function load(){setError("");if(!organizationId){setAllowed(false);setOfficials([]);return}const{data:u}=await supabase.auth.getUser();if(!u.user)return;const{data:memberships}=await supabase.from("organization_memberships").select("role").eq("organization_id",organizationId).eq("user_id",u.user.id);const ok=(memberships||[]).some(m=>["owner","admin","assignor"].includes(m.role));setAllowed(ok);if(!ok)return;const result=await supabase.rpc("get_organization_official_directory",{p_organization_id:organizationId});if(result.error)setError(result.error.message);else setOfficials((result.data||[]) as Official[])}
 useEffect(()=>{void load()},[organizationId]);
 function downloadCurrent(){const head=COLUMNS.map(([label])=>esc(label)).join(","),lines=officials.map(o=>COLUMNS.map(([,key])=>esc(key==="ussf_id"?o.id:key==="date_of_birth"||key.includes("date")?dateValue(o[key]):o[key])).join(","));const blob=new Blob([[head,...lines].join("\n")],{type:"text/csv;charset=utf-8"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download="refassign-referee-profiles.csv";a.click();URL.revokeObjectURL(url)}
 function validate(raw:string[][]):Row[]{if(!raw.length)throw new Error("Import file is empty.");const header=raw[0].map(cell=>aliases[normalize(String(cell))]);const missing=COLUMNS.filter(([,key])=>!header.includes(key)).map(([label])=>label);if(missing.length)throw new Error(`Missing column(s): ${missing.join(", ")}`);return raw.slice(1).map((values,index)=>{const record=Object.fromEntries(COLUMNS.map(([,key])=>[key,String(values[header.indexOf(key)]??"").trim()])) as Record<Key,string>,issues:string[]=[];if(!record.first_name)issues.push("First name is required");if(!record.last_name)issues.push("Last name is required");if(record.email&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(record.email))issues.push("Email is invalid");if(record.secondary_email&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(record.secondary_email))issues.push("Secondary email is invalid");if(!booleanValid(record.is_minor))issues.push("is a minor must be true or false");for(const[,key]of COLUMNS.filter(([,key])=>key==="date_of_birth"||key.includes("date"))){if(!validDate(record[key]))issues.push(`${COLUMNS.find(([,k])=>k===key)?.[0]} must use YYYY-MM-DD`)}if(record.referee_years_experience&&(!Number.isInteger(Number(record.referee_years_experience))||Number(record.referee_years_experience)<0))issues.push("Referee years exp must be zero or greater");const id=record.ussf_id&&officials.some(o=>o.id===record.ussf_id)?record.ussf_id:officials.find(o=>String(o.email||"").toLowerCase()===record.email.toLowerCase())?.id||"";return{...record,row:index+2,id,action:id?"Update":"Add",valid:!issues.length,issue:issues.join("; ")}})}
 async function handleFile(event:ChangeEvent<HTMLInputElement>){setRows([]);setMessage("");setError("");const file=event.target.files?.[0];if(!file)return;try{const buffer=await file.arrayBuffer();let raw:string[][]|null=null;try{const XLSX=await import("xlsx"),workbook=XLSX.read(buffer,{type:"array",cellDates:true}),sheet=workbook.Sheets[workbook.SheetNames[0]];raw=XLSX.utils.sheet_to_json(sheet,{header:1,defval:"",raw:false,dateNF:"yyyy-mm-dd"}) as string[][]}catch{}if(!raw?.length)raw=parseCSV(new TextDecoder("utf-8").decode(buffer));setRows(validate(raw))}catch(x){setError(x instanceof Error?x.message:"Unable to read referee profile file.")}finally{event.target.value=""}}
 async function applyImport(){if(!organizationId||!rows.length||rows.some(r=>!r.valid))return;setImporting(true);setError("");let added=0,updated=0;try{for(const row of rows){const nullable=(key:Key)=>row[key]||null;const{data,error:rowError}=await supabase.rpc("upsert_my_referee_profile_row",{p_organization_id:organizationId,p_ussf_id:nullable("ussf_id"),p_first_name:row.first_name,p_last_name:row.last_name,p_date_of_birth:nullable("date_of_birth"),p_is_minor:booleanValue(row.is_minor),p_gender:nullable("gender"),p_ethnicity:nullable("ethnicity"),p_email:nullable("email"),p_secondary_email:nullable("secondary_email"),p_address:nullable("home_address"),p_address_unit:nullable("address_unit"),p_city:nullable("home_city"),p_state:nullable("home_state"),p_zip:nullable("home_zip"),p_country:nullable("country"),p_phone:nullable("phone"),p_mobile_phone:nullable("mobile_phone"),p_license:nullable("license"),p_status:nullable("license_status"),p_issue_date:nullable("license_issue_date"),p_expiration_date:nullable("license_expiration_date"),p_issuer:nullable("license_issuer"),p_curriculum:nullable("curriculum"),p_background_screening:nullable("background_screening"),p_background_screening_expiration_date:nullable("background_screening_expiration_date"),p_safesport:nullable("safesport"),p_safesport_expiration_date:nullable("safesport_expiration_date"),p_intro_player_safety:nullable("intro_player_safety"),p_intro_player_safety_expiration_date:nullable("intro_player_safety_expiration_date"),p_safe_soccer:nullable("safe_soccer"),p_safe_soccer_expiration_date:nullable("safe_soccer_expiration_date"),p_provisional_status:nullable("provisional_status"),p_referee_years_experience:row.referee_years_experience?Number(row.referee_years_experience):null});if(rowError)throw new Error(`Row ${row.row} (${row.first_name} ${row.last_name}): ${rowError.message}`);const result=Array.isArray(data)?data[0]:data;if(result?.result_action==="Add")added++;else updated++}setMessage(`Import complete: ${updated} updated, ${added} added.`);setRows([]);await load()}catch(x){setError(x instanceof Error?x.message:"Roster import failed")}finally{setImporting(false)}}
 if(!allowed)return <section className="card"><h2>Roster Import / Export</h2><p>Only Administrators and Assignors can use the master roster uploader.</p></section>;
 const invalid=rows.filter(row=>!row.valid).length;
 return <section className="card"><div className="cardHead"><div><h2>Referee Profile Import / Export</h2><p>USSF-ID is the existing RefAssign referee ID. Uploading it updates that referee; leave it blank for a new referee.</p></div><button className="secondary" onClick={downloadCurrent}>Download Current Referees</button></div><input type="file" accept=".xlsx,.xls,.csv" onChange={handleFile}/><p><small>All 33 profile columns must remain in the file and in the exported order. Dates use YYYY-MM-DD.</small></p>{error&&<div className="errorBox">{error}</div>}{message&&<div className="loginMessage">{message}</div>}{rows.length>0&&<><p>{rows.length} rows • {rows.filter(r=>r.action==="Update").length} updates • {rows.filter(r=>r.action==="Add").length} additions • {invalid} errors</p><div className="tableWrap"><table><thead><tr><th>Row</th><th>Action</th><th>USSF-ID</th><th>Referee</th><th>Email</th><th>License</th><th>Status</th><th>Validation</th></tr></thead><tbody>{rows.map(row=><tr key={row.row}><td>{row.row}</td><td>{row.action}</td><td>{row.ussf_id||"New"}</td><td><b>{row.first_name} {row.last_name}</b></td><td>{row.email}</td><td>{row.license}</td><td>{row.license_status}</td><td><span className={`badge ${row.valid?"green":"red"}`}>{row.valid?"Ready":row.issue}</span></td></tr>)}</tbody></table></div><button className="primary" disabled={importing||invalid>0} onClick={()=>void applyImport()}>{importing?"Updating Referees…":"Apply Referee Changes"}</button></>}</section>
}
