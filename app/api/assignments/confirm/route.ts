import {NextRequest,NextResponse} from 'next/server'
import {createServerSupabaseClient} from '../../../../lib/supabase/server'
function esc(v:unknown){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]!))}
export async function POST(req:NextRequest){const supabase=await createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const {data:profile}=await supabase.from('profiles').select('role').eq('id',user.id).maybeSingle();if(!['admin','assignor'].includes(profile?.role||''))return NextResponse.json({error:'Only Administrators and Assignors can confirm assignments.'},{status:403});const body=await req.json().catch(()=>({})) as {assignmentId?:string};if(!body.assignmentId)return NextResponse.json({error:'Assignment is required.'},{status:400});const {data:raw,error}=await supabase.from('assignments').select('id,status,published_at,officials(first_name,last_name,email),sport_positions(name),games(game_number,starts_at,duration_minutes,notes,home:teams!games_home_team_id_fkey(name),away:teams!games_away_team_id_fkey(name),location:locations(name,address,city,state),leagues(name),levels(name))').eq('id',body.assignmentId).maybeSingle();if(error||!raw)return NextResponse.json({error:error?.message||'Assignment not found.'},{status:404});const a=raw as any;if(!a.published_at)return NextResponse.json({error:'This assignment must be published before it can be confirmed.'},{status:400});if(a.status==='declined')return NextResponse.json({error:'A declined assignment cannot be confirmed.'},{status:400});const o=Array.isArray(a.officials)?a.officials[0]:a.officials,g=Array.isArray(a.games)?a.games[0]:a.games,pos=Array.isArray(a.sport_positions)?a.sport_positions[0]:a.sport_positions;if(!o?.email)return NextResponse.json({error:'The official does not have an email address.'},{status:400});const apiKey=process.env.RESEND_API_KEY;if(!apiKey)return NextResponse.json({error:'RESEND_API_KEY is not configured.'},{status:500});const home=(Array.isArray(g?.home)?g.home[0]:g?.home)?.name||'TBD',away=(Array.isArray(g?.away)?g.away[0]:g?.away)?.name||'TBD',loc=Array.isArray(g?.location)?g.location[0]:g?.location,league=Array.isArray(g?.leagues)?g.leagues[0]:g?.leagues,level=Array.isArray(g?.levels)?g.levels[0]:g?.levels;const when=new Date(g.starts_at).toLocaleString('en-US',{timeZone:'America/Chicago',weekday:'long',month:'long',day:'numeric',year:'numeric',hour:'numeric',minute:'2-digit',timeZoneName:'short'});const address=[loc?.address,loc?.city,loc?.state].filter(Boolean).join(', ');const html=`<div style="font-family:Arial,sans-serif;background:#f5f7fb;padding:28px"><div style="max-width:620px;margin:auto;background:white;border:1px solid #e2e8f0;border-radius:14px;overflow:hidden"><div style="background:#14233b;color:white;padding:24px 28px"><div style="font-size:22px;font-weight:800">REF<span style="color:#60a5fa">ASSIGN</span></div><div style="font-size:12px;color:#cbd5e1;margin-top:4px">Confirmed Game Assignment</div></div><div style="padding:28px"><p style="font-size:16px;color:#172033">Hi ${esc(o.first_name)},</p><p style="color:#475569">Your assignment has been <b>confirmed</b> by Ref Pro Group. Please use the information below as your game reminder.</p><h2 style="color:#172033;margin:24px 0 18px">${esc(home)} vs ${esc(away)}</h2><table style="width:100%;border-collapse:collapse;color:#172033;font-size:14px"><tr><td style="padding:9px 0;color:#64748b">Game Number</td><td style="font-weight:700">${esc(g.game_number||'—')}</td></tr><tr><td style="padding:9px 0;color:#64748b">Date & Time</td><td style="font-weight:700">${esc(when)}</td></tr><tr><td style="padding:9px 0;color:#64748b">Position</td><td style="font-weight:700">${esc(pos?.name||'Official')}</td></tr><tr><td style="padding:9px 0;color:#64748b">Location</td><td style="font-weight:700">${esc(loc?.name||'TBD')}${address?`<br><span style="font-weight:400;color:#64748b">${esc(address)}</span>`:''}</td></tr><tr><td style="padding:9px 0;color:#64748b">League / Level</td><td style="font-weight:700">${esc(league?.name||'—')}${level?.name?` • ${esc(level.name)}`:''}</td></tr><tr><td style="padding:9px 0;color:#64748b">Game Length</td><td style="font-weight:700">${esc(g.duration_minutes||110)} minutes</td></tr></table>${g?.notes?`<div style="margin-top:18px;padding:14px;background:#f8fafc;border-radius:8px"><b>Game Information</b><div style="margin-top:5px;color:#475569">${esc(g.notes)}</div></div>`:''}<div style="margin-top:22px;padding:14px;background:#ecfdf5;border:1px solid #a7f3d0;border-radius:8px;color:#065f46"><b>This assignment is confirmed. No additional acceptance is required.</b></div></div></div><div style="text-align:center;color:#94a3b8;font-size:10px;padding:18px">© 2026 Ref Pro Group, LLC. All rights reserved.</div></div>`;const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json','Idempotency-Key':`confirmation-${a.id}-${a.published_at}`},body:JSON.stringify({from:'RefAssign <notifications@assignments.ref-assign.com>',to:[o.email],reply_to:'assignments@ref-assign.com',subject:`Confirmed Game: ${home} vs ${away}`,html})});const result=await response.json().catch(()=>({})) as {id?:string;message?:string};if(!response.ok)return NextResponse.json({error:result.message||`Unable to send confirmation email (${response.status}).`},{status:502});const {error:updateError}=await supabase.from('assignments').update({status:'confirmed',responded_at:new Date().toISOString(),decline_reason:null}).eq('id',a.id);if(updateError)return NextResponse.json({error:updateError.message},{status:400});return NextResponse.json({confirmed:true,emailId:result.id||null})}
