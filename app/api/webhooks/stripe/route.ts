import {createHmac,timingSafeEqual} from "node:crypto";import {NextResponse} from "next/server";import {createClient} from "@supabase/supabase-js";
const escapeHtml=(value:unknown)=>String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]!));
function validSignature(payload:string,header:string,secret:string){const parts=Object.fromEntries(header.split(",").map(p=>p.split("=",2) as [string,string])),timestamp=parts.t,signature=parts.v1;if(!timestamp||!signature||Math.abs(Date.now()/1000-Number(timestamp))>300)return false;const expected=createHmac("sha256",secret).update(`${timestamp}.${payload}`).digest("hex");try{return timingSafeEqual(Buffer.from(expected,"hex"),Buffer.from(signature,"hex"))}catch{return false}}
export async function POST(request:Request){const secret=process.env.STRIPE_WEBHOOK_SECRET,serviceKey=process.env.SUPABASE_SERVICE_ROLE_KEY,stripeKey=process.env.STRIPE_SECRET_KEY;if(!secret||!serviceKey)return NextResponse.json({error:"Stripe webhook is not configured."},{status:503});const payload=await request.text(),signature=request.headers.get("stripe-signature")||"";if(!validSignature(payload,signature,secret))return NextResponse.json({error:"Invalid Stripe signature."},{status:400});let event:any;try{event=JSON.parse(payload)}catch{return NextResponse.json({error:"Invalid payload."},{status:400})}const supabase=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,serviceKey,{auth:{persistSession:false,autoRefreshToken:false}}),obj=event.data?.object||{},now=new Date().toISOString();
// Organization subscription checkout: trials are complete Checkout sessions even though payment_status can be no_payment_required.
if(event.type==="checkout.session.completed"&&obj.metadata?.refassign_subscription_id){const id=obj.metadata.refassign_subscription_id;await supabase.from("refassign_subscriptions").update({stripe_checkout_session_id:obj.id,stripe_customer_id:typeof obj.customer==="string"?obj.customer:null,stripe_subscription_id:typeof obj.subscription==="string"?obj.subscription:null,status:"trialing",trial_ends_at:new Date(Date.now()+14*86400000).toISOString(),updated_at:now}).eq("id",id);return NextResponse.json({received:true})}
if(["customer.subscription.created","customer.subscription.updated","customer.subscription.deleted"].includes(event.type)){const id=obj.metadata?.refassign_subscription_id;if(id){await supabase.from("refassign_subscriptions").update({stripe_customer_id:typeof obj.customer==="string"?obj.customer:null,stripe_subscription_id:obj.id,status:obj.status||"unknown",trial_ends_at:obj.trial_end?new Date(obj.trial_end*1000).toISOString():null,current_period_end:obj.current_period_end?new Date(obj.current_period_end*1000).toISOString():null,cancel_at_period_end:Boolean(obj.cancel_at_period_end),updated_at:now}).eq("id",id)}return NextResponse.json({received:true})}
// Existing official-registration payment flow remains separate from RefAssign organization subscriptions.
if(event.type!=="checkout.session.completed"||obj.payment_status!=="paid")return NextResponse.json({received:true});const registrationId=obj.metadata?.registration_id;if(!registrationId)return NextResponse.json({received:true});const{data:registration,error}=await supabase.from("official_registrations").update({status:"paid",payment_status:"paid",stripe_checkout_session_id:obj.id,stripe_payment_intent_id:obj.payment_intent||null,paid_at:now,updated_at:now}).eq("id",registrationId).select("id,first_name,last_name,email,registration_year,registrar_notified_at,registration_program_id,registration_programs(name)").single();if(error)return NextResponse.json({error:error.message},{status:500});if(!registration.registrar_notified_at&&process.env.RESEND_API_KEY){const[{data:roles},{data:owners}]=await Promise.all([supabase.from("registration_program_staff").select("user_id").eq("program_id",registration.registration_program_id),supabase.from("protected_accounts").select("user_id")]),emails:string[]=[];for(const role of [...(roles||[]),...(owners||[])]){const{data:userData}=await supabase.auth.admin.getUserById(role.user_id);if(userData.user?.email)emails.push(userData.user.email)}const unique=[...new Set(emails)];if(unique.length){const emailResponse=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`,"Content-Type":"application/json","Idempotency-Key":`registrar-registration-${registration.id}`},body:JSON.stringify({from:"RefAssign <notifications@assignments.ref-assign.com>",to:unique,reply_to:"assignments@ref-assign.com",subject:`New paid official registration: ${registration.first_name} ${registration.last_name}`,html:`<div style="font-family:Arial,sans-serif"><h2>New Official Registration</h2><p><b>${escapeHtml(registration.first_name)} ${escapeHtml(registration.last_name)}</b> has completed the ${escapeHtml(registration.registration_year)} registration payment for ${escapeHtml(((registration.registration_programs as unknown as {name?:string}|null)?.name)||"Iowa Soccer")}.</p><p>${escapeHtml(registration.email)}</p><p>Sign in to RefAssign and open Registrar to approve eligibility.</p></div>`})});if(emailResponse.ok)await supabase.from("official_registrations").update({registrar_notified_at:now}).eq("id",registration.id)}}return NextResponse.json({received:true})}
