"use client";
import {FormEvent,useMemo,useState} from "react";
import {createClient} from "../../lib/supabase/client";
export default function KitRequestPage(){const supabase=useMemo(()=>createClient(),[]),[busy,setBusy]=useState(false),[error,setError]=useState(""),[done,setDone]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget),{error:err}=await supabase.from("kit_requests").insert({first_name:f.get("first_name"),last_name:f.get("last_name"),email:f.get("email"),phone:f.get("phone")||null,address:f.get("address"),city:f.get("city"),state:f.get("state"),zip:f.get("zip"),t_shirt_size:f.get("t_shirt_size")});if(err)setError(err.message);else setDone(true);setBusy(false)}if(done)return <main className="publicRegistration"><section className="card"><div className="brand">Ref<span>Assign</span></div><h1>Kit Request Received</h1><div className="loginMessage">Your Iowa Soccer referee kit request has been submitted.</div></section></main>;return <main className="publicRegistration"><section className="card"><div className="brand">Ref<span>Assign</span></div><h1>Iowa Soccer Referee Kit Request</h1><p>Register to have a referee kit sent to you. Please enter the mailing address where the kit should be delivered.</p>{error&&<div className="errorBox">{error}</div>}<form className="officialForm" onSubmit={submit}><label>First Name<input name="first_name" required maxLength={80}/></label><label>Last Name<input name="last_name" required maxLength={80}/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone" type="tel"/></label><label className="fullSpan">Mailing Address<input name="address" required/></label><label>City<input name="city" required/></label><label>State<input name="state" defaultValue="IA" required/></label><label>ZIP<input name="zip" inputMode="numeric" required/></label><label>T-Shirt Size<select name="t_shirt_size" required defaultValue=""><option value="" disabled>Select size</option><option value="XS">Extra Small</option><option value="S">Small</option><option value="M">Medium</option><option value="L">Large</option><option value="XL">Extra Large</option><option value="2XL">Double Extra Large</option><option value="3XL">Triple Extra Large</option></select></label><button className="primary" disabled={busy}>{busy?"Submitting…":"Submit Kit Request"}</button></form><p><a href="/register">Return to Registration</a></p></section></main>}
